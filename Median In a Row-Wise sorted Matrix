#include <bits/stdc++.h>
using namespace std;

int main() {
	//code
	int t; cin>>t;
	while(t--){
	    int n,m; cin>>n>>m;
	    
	    int a[n*m];
	    for(int i=0;i<n*m;i++)
	    {
	        cin>>a[i];
	    }
	    sort(a,a+n*m);
	    cout<<a[n*m/2]<<endl;
	    
	}
	return 0;
}
